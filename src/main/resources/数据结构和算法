数据结构和算法笔记
第一次学习总结：
数据结构的整体框架：
数据结构与算法基础：
     线性结构：数组、栈、队列、单链表、循环链表、双链表、递归
     排序算法：八种常见的排序算法【交换排序：冒泡排序和快速排序；[1-2]
                                插入排序：直接插入排序和希尔排序；[3-4]
                                选择排序：简单选择排序和堆排序；[5-6]
                                归并排序；[7]
                                基数排序；[8]】
     树结构：
     哈希表：
     图结构：

  1.常见的数据结构：队列、树、堆、数组、栈、链表、图、散列表
  线性结构：数组、栈、队列、链表、递归
   1.1 数组结构  数组是一种线性结构
       增删慢，查询快
   1.2 栈 是一种运算受限的线性表,后进先出(LIFO)
       程序中什么是使用栈实现的呢?
       a.我们知道函数之间和相互调用: A调用B, B中又调用C, C中又调用D.
       b.那样在执行的过程中, 会先将A压入栈, A没有执行完, 所有不会弹出栈.
       c.在A执行的过程中调用了B, 会将B压入到栈, 这个时候B在栈顶, A在栈底.
       d.如果这个时候B可以执行完, 那么B会弹出栈. 但是B有执行完吗? 没有, 它调用了C.
       e.所以C会压栈, 并且在栈顶. 而C调用了D, D会压入到栈顶.
       f.所以当前的栈顺序是: 栈底A->B->C->D栈顶
       g.D执行完, 弹出栈. C/B/A依次弹出栈.

